pipeline {
    agent any

    tools {
        maven 'Maven'
        jdk 'JDK'
    }

    environment {
        APP_NAME = "Hi-Java-maven"
        DOCKER_IMAGE = "maven-docker-image"
        DOCKER_TAG = "${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('check-Maven-Build') {
            steps {
                sh 'mvn clean compile'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }

        stage('Package') {
            steps {
                sh 'mvn package'
            }
            post {
                success {
                    archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
                }
            }
        }

        stage('Docker Build') {
            steps {
                sh '''
                  docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} .
                  docker tag ${DOCKER_IMAGE}:${DOCKER_TAG} ${DOCKER_IMAGE}:latest
                '''
            }
        }

        stage('Docker Image Verify') {
            steps {
                sh 'docker images | grep Hi-Java-maven'
            }
        }
        stage ('Pipeline Success status'){
            post {
        success {
            echo "✅ Pipeline SUCCESS: Build #${BUILD_NUMBER}"
        }
        failure {
            echo "❌ Pipeline FAILED: Check logs"
        }
            }
        }   
    
        cleanup {
            cleanWs()
        }
    }
}
